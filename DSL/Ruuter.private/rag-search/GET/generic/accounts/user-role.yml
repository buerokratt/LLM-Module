declaration:
  call: declare
  version: 0.1
  description: "Local development only: mocked user role, bypasses TIM/cookie auth"
  method: get
  accepts: json
  returns: json
  namespace: rag-search

step_1:
  call: reflect.mock
  args:
    response:
      response:
        - "ROLE_ADMINISTRATOR"
  result: reflected_request

step_2:
  return: ${reflected_request.response.body}
